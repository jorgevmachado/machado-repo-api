version: '3.8'

services:
  postgres:
    container_name: machado-repo-api_db
    image: postgres:latest
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: machadoRepoDB
      POSTGRES_PASSWORD: machadoRepoDB
      POSTGRES_DB: machadoRepoDB
    volumes:
      - postgres_data:/var/lib/postgresql/data      
    restart: unless-stopped
  redis:
    image: redis:latest
    container_name: machado-repo-api_redis
    ports:
      - "6379:6379"
    restart: always
volumes:
  postgres_data:
